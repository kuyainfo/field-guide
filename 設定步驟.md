# 百濟探索圖鑑｜設定步驟

三件事要在你的帳號裡按，大約 10 分鐘。做完把兩樣東西給我（或自己貼進檔案），系統就通了。

---

## 第一步：建 Drive 資料夾（2 分鐘）

1. 打開 Google 雲端硬碟，新增資料夾，取名例如 `百濟探索圖鑑_1017`
2. 在資料夾上按右鍵 →「共用」→「一般存取權」改成 **知道連結的任何人**、角色 **檢視者**
3. 進入這個資料夾，看網址列最後一段，複製起來

   網址 `https://drive.google.com/drive/folders/1AbCdEf...XyZ`
   要的就是 `1AbCdEf...XyZ` 這一段，這是**資料夾 ID**

> 沒設成「知道連結的任何人」，後台會看不到縮圖。

---

## 第二步：部署 Apps Script（5 分鐘）

1. 打開 https://script.google.com/ ，按「新增專案」
2. 把 `upload.gs` 的內容整個貼進去，覆蓋原本的 `function myFunction(){}`
3. 找到第一行 `var FOLDER_ID = "在這裡貼上資料夾 ID";`，換成剛才複製的資料夾 ID
4. 按右上角「部署」→「新增部署作業」
5. 齒輪圖示選 **網頁應用程式**，然後：
   - 執行身分：**我**
   - 具有存取權的使用者：**任何人**
6. 按「部署」，第一次會跳授權，按「授予存取權」

   中間會出現「Google 尚未驗證這個應用程式」的警告，按 **進階** →「前往（專案名稱）(不安全)」。這是你自己寫的腳本，不是外人的。
7. 複製最後給你的**網頁應用程式網址**，格式 `https://script.google.com/macros/s/AKfy.../exec`

**測試**：把那段網址直接貼進瀏覽器打開，看到 `{"ok":true,"alive":true,...}` 就成功了。

---

## 第三步：貼回程式（1 分鐘）

打開 `index.html`，找到最上面這一行：

```js
var UPLOAD_URL = "";
```

把第二步的網址貼進引號中間，存檔。這是唯一要改的地方。

---

## 第四步：Firebase 規則（2 分鐘）

Firebase 用你現有的 `kuya-candy-game` 專案，**不用建新的**，設定已經寫在程式裡了。
只要確認規則允許讀寫：

1. 打開 https://console.firebase.google.com/ ，進入 `kuya-candy-game`
2. 左側 Realtime Database →「規則」頁籤
3. 確認 `fieldguide_beone_1017` 這個節點可以讀寫。若你現有規則是整個開放的，就不用動；若是逐節點列的，補上這段：

```json
"fieldguide_beone_1017": {
  ".read": true,
  ".write": true
}
```

4. 按「發布」

> 規則有到期日。活動前一週記得回來確認沒有過期，這是每次活動都踩過的坑。

---

## 檔案說明

| 檔案 | 誰用 | 說明 |
|------|------|------|
| `index.html` | 參加者 | 玩家端，QR 就指這支 |
| `admin.html` | 工作人員 | 審核後台，密碼 `kuya2026` |
| `upload.gs` | — | 貼進 Apps Script 用的，不放網站上 |

---

## 現場注意事項

**QR 要提醒用外部瀏覽器開。** 從 LINE 掃進去會用 LINE 內建瀏覽器，相機常常叫不起來。程式會自動偵測並跳提示，但最好在說明時就講一次。

**收訊不好是常態。** 拍完照片會先在本機排隊，背景自動重試，玩家不用等；總覽頁會顯示「有 N 張正在背景上傳」。

**照片沒過要讓對方知道。** 退回的卡片會變紅色並顯示「退回，請重拍」，但玩家不一定會回頭看，現場廣播或工作人員口頭提醒比較保險。

**後台有「這組全部通過」。** 老街尖峰時段照片會塞車，來不及逐張看就先整組放行，事後再抽查。

---

## 目前還沒做的事

- [ ] 獎勵品項文字（`index.html` 的 `PRIZES` 陣列，兩階段 8／12）
- [ ] 博物館館內兩題，等再次場勘後確認物件
- [ ] 老街台車站壁畫的確切位置
- [ ] 領獎核銷（後台目前只做審核，還沒有「已領取」按鈕）
- [ ] 推上 GitHub
